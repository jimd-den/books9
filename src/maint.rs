//
// libbiz::maint -- maint (entity).
//
// Phase 7 lands the full surface. This commit ships the
// plan doc + a stub so the test file in
// `tests/maint_*.rs` can compile. The body lands in the
// next commit.
